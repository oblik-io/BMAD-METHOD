# Персональне налаштування форку (одноразово)

## 🎯 Мета

Налаштувати automation для sync з upstream БЕЗ засмічення main гілки.

---

## 📋 Кроки налаштування

### 1. Зробити скрипт executable

```bash
chmod +x ~/bin/git-sync-fork
```

### 2. Додати ~/bin до PATH (якщо ще немає)

**Перевірити:**

```bash
echo $PATH | grep "$HOME/bin"
```

**Якщо пусто, додати:**

Для **zsh** (macOS default):

```bash
echo 'export PATH="$HOME/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

Для **bash**:

```bash
echo 'export PATH="$HOME/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```

### 3. Перевірити що працює

```bash
which git-sync-fork
# Має показати: $HOME/bin/git-sync-fork

git-sync-fork check
# Має показати статус sync
```

---

## 🚀 Використання

### Щоденний workflow

**Ранок (перед роботою):**

```bash
cd /path/to/BMAD-METHOD
git-sync-fork check
```

**Якщо є оновлення:**

```bash
git-sync-fork sync
```

**В OpenCode:**

```
/sync-upstream
```

---

## ✅ Що налаштовано

### Глобальні файли (поза git):

- ✅ `~/.config/opencode/command/sync-upstream.md` - OpenCode command
- ✅ `~/bin/git-sync-fork` - CLI sync script

### Локальна документація (можна комітити):

- ✅ `.github/FORK-WORKFLOW.md` - best practices
- ✅ `.github/SETUP.md` - ця інструкція

### Що НЕ створено:

- ❌ `.opencode/` в проєкті - використовується глобальна
- ❌ `Makefile` в проєкті - замінений на `~/bin/git-sync-fork`
- ❌ Файли в `main` гілці - main залишається чистим

---

## 🔍 Перевірка правильності

```bash
# 1. Main чистий?
git log origin/main ^upstream/main
# Має бути ПОРОЖНЬО

# 2. Можу синхронізуватися?
git-sync-fork check
# Має працювати

# 3. OpenCode command працює?
# В OpenCode: /sync-upstream
# Має показати команду
```

---

## 📚 Наступні кроки

1. Прочитати `.github/FORK-WORKFLOW.md` - best practices
2. Налаштувати git aliases (опціонально)
3. Створити feature гілку для своїх змін
4. Ніколи не модифікувати `main`

---

## 🆘 Допомога

**Скрипт не працює:**

```bash
# Перевірити права
ls -l ~/bin/git-sync-fork

# Має бути: -rwxr-xr-x (x = executable)
# Якщо ні:
chmod +x ~/bin/git-sync-fork
```

**OpenCode не бачить command:**

```bash
# Перевірити файл
ls ~/.config/opencode/command/sync-upstream.md

# Перезапустити OpenCode
```

**PATH не працює:**

```bash
# Додати вручну в поточній сесії
export PATH="$HOME/bin:$PATH"

# Потім додати в shell config
```
